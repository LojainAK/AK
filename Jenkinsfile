pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Add commands to build your application
                sh 'python app.py'
            }
        }
        // Add more stages as needed, e.g., Test, Deploy
    }

}

}
